# drmem-drv-hue

This monitor controls devices through a Philips Hue hub.

## Configuration

The driver needs several parameters that are common to the hub and an array of paramaters for each of the device that are to be controlled.

- `addr`
- `app_id`
- `devices`