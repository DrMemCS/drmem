[workspace]
members = [
    "drmem-api",
    "drivers/*",
    "backends/*",
    "drmemd"
]

# Profiles used by all binaries and packages.

[profile.release]
opt-level = 3
debug = false
lto = "thin"
debug-assertions = false
strip = "debuginfo"
