[workspace]
members = ["drmem-driver-api", "drmem-config", "backends/*", "drmemd"]

# Profiles used by all binaries and packages.

[profile.release]
opt-level = 3
debug = false
lto = true
debug-assertions = false
